"use client";

import TargetCompressClient from "@/components/TargetCompressClient";

export default function CompressTo50KBPage() {
  return (
    <TargetCompressClient
      defaultTargetKB={50}
      title="Compress Image to 50KB Online Free"
      subtitle="Reduce JPG, PNG, and WEBP image file sizes to strictly under 50KB. Perfect for UPSC, SSC, government exam forms, job portals, and visa applications."
    />
  );
}
