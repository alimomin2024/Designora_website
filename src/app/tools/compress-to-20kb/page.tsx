"use client";

import TargetCompressClient from "@/components/TargetCompressClient";

export default function CompressTo20KBPage() {
  return (
    <TargetCompressClient
      defaultTargetKB={20}
      title="Compress Image to 20KB Online Free"
      subtitle="Reduce photo and signature sizes to strictly under 20KB for competitive exam forms, banking portals, and official identity documents."
    />
  );
}
